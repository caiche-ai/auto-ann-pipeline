"""GroundingDINO detection stage.

The package initializer stays lightweight so API schema imports never load
model dependencies. Import adapters, settings, and workers from their explicit
submodules.
"""
